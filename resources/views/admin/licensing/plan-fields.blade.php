<label>Nome</label><input name="name" required value="{{ old('name',$plan?->name) }}"><label>Slug</label><input name="slug" required value="{{ old('slug',$plan?->slug) }}" placeholder="starter"><label>Descrizione</label><textarea name="description">{{ old('description',$plan?->description) }}</textarea><label>Prezzo annuale in centesimi</label><input type="number" min="0" name="annual_price_cents" required value="{{ old('annual_price_cents',$plan?->annual_price_cents ?? 0) }}"><label>Valuta</label><input name="currency" maxlength="3" required value="{{ old('currency',$plan?->currency ?? 'EUR') }}"><label>Utenti inclusi, owner compreso</label><input type="number" min="1" name="seat_limit" required value="{{ old('seat_limit',$plan?->seat_limit ?? 1) }}"><label>Lead mensili (vuoto = illimitati)</label><input type="number" min="1" name="monthly_lead_limit" value="{{ old('monthly_lead_limit',$plan?->monthly_lead_limit) }}"><label>Token AI mensili (vuoto = illimitati)</label><input type="number" min="1" name="monthly_ai_token_limit" value="{{ old('monthly_ai_token_limit',$plan?->monthly_ai_token_limit) }}"><label>Funzioni, una per riga</label><textarea name="features_text" rows="4">{{ old('features_text',implode("\n",$plan?->features ?? [])) }}</textarea><label>Stripe Price ID annuale</label><input name="stripe_price_id" value="{{ old('stripe_price_id',$plan?->stripe_price_id) }}" placeholder="price_..."><label>Ordine</label><input type="number" min="0" name="sort_order" required value="{{ old('sort_order',$plan?->sort_order ?? 0) }}"><label><input style="width:auto" type="checkbox" name="is_active" value="1" @checked(old('is_active',$plan?->is_active))> Pubblicabile e acquistabile</label>

